<section class="borderdetails">
    <% if (title) { %>
    <h1 class="title"><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>

    <%# messages() %>

    <%- grid %>

</section>